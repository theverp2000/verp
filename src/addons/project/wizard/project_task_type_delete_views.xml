<?xml version="1.0" encoding="utf-8"?>
<verp>
    <record id="viewProjectTaskTypeDeleteWizard" model="ir.ui.view">
        <field name="label">project.task.type.delete.wizard.form</field>
        <field name="model">project.task.type.delete.wizard</field>
        <field name="arch" type="xml">
            <form string="Delete Stage">
                <field name="tasksCount" invisible="1" />
                <field name="stagesActive" invisible="1" />
                <div attrs="{'invisible': [['tasksCount', '>', 0]]}">
                    <p>Are you sure you want to delete those stages ?</p>
                </div>
                <div attrs="{'invisible': ['|', ['stagesActive', '=', false], ['tasksCount', '=', 0]]}">
                    <p>You cannot delete stages containing tasks. You can either archive them or first delete all of their tasks.</p>
                </div>
                <div attrs="{'invisible': ['|', ['stagesActive', '=', true], ['tasksCount', '=', 0]]}">
                    <p>You cannot delete stages containing tasks. You should first delete all of their tasks.</p>
                </div>
                <footer>
                    <button string="Archive Stages" type="object" name="actionArchive" class="btn btn-primary" attrs="{'invisible': ['|', ['stagesActive', '=', false], ['tasksCount', '=', 0]]}" data-hotkey="q"/>
                    <button string="Delete" type="object" name="actionUnlink" class="btn btn-primary" attrs="{'invisible': [['tasksCount', '>', 0]]}" data-hotkey="w"/>
                    <button string="Discard" special="cancel" data-hotkey="z" />
                </footer>
            </form>
        </field>
    </record>

    <record id="viewProjectTaskTypeDeleteConfirmationWizard" model="ir.ui.view">
        <field name="label">project.task.type.delete.wizard.form</field>
        <field name="model">project.task.type.delete.wizard</field>
        <field name="arch" type="xml">
            <form string="Delete Stage">
                <div>
                    <p>This will archive the stages and all the tasks they contain from the following projects:</p>
                    <field name="projectIds" readonly="1">
                        <tree>
                            <field name="label"/>
                        </tree>
                    </field>
                    <p>Are you sure you want to continue?</p>
                </div>
                <footer>
                    <button string="Confirm" type="object" name="actionConfirm" class="btn btn-primary" data-hotkey="q"/>
                    <button string="Discard" special="cancel" data-hotkey="z" />
                </footer>
            </form>
        </field>
    </record>
</verp>
