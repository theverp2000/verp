<?xml version="1.0" encoding="utf-8"?>
<verp>
    <data>
        <record id="projectDeleteWizardForm" model="ir.ui.view">
            <field name="label">Delete Project</field>
            <field name="model">project.delete.wizard</field>
            <field name="arch" type="xml">
                <form string="Delete Project">
                    <field name="taskCount" invisible="1" />
                    <field name="projectsArchived" invisible="1" />
                    <group attrs="{'invisible': [['taskCount', '=', 0]]}">
                        <span>You cannot delete a project containing tasks. You can either archive it or first delete all of its tasks.</span>
                    </group>
                    <group attrs="{'invisible': [['taskCount', '>', 0]]}">
                        <span>Are you sure you want to delete this project ?</span>
                    </group>
                    <footer attrs="{'invisible': [['taskCount', '=', 0]]}">
                        <button string="Archive project" type="object" name="actionArchive" class="btn btn-primary" attrs="{'invisible': [['projectsArchived', '=', true]]}" data-hotkey="q"/>
                        <button string="Discard" special="cancel" data-hotkey="z" />
                    </footer>
                    <footer attrs="{'invisible': [['taskCount', '>', 0]]}">
                        <button string="Ok" type="object" name="confirmDelete" class="btn btn-primary" data-hotkey="q"/>
                        <button string="Cancel" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</verp>
