<?xml version="1.0" encoding="utf-8"?>
<verp>

    <record id="projectShareWizardViewForm" model="ir.ui.view">
        <field name="label">project.share.wizard.view.form</field>
        <field name="model">project.share.wizard</field>
        <field name="arch" type="xml">
            <form string="Share Project">
                <field name="resModel" invisible="1"/>
                <field name="resId" invisible="1"/>
                <field name="displayAccessMode" invisible="1" />
                <p class="alert alert-warning" attrs="{'invisible': [['accessWarning', '=', '']]}" role="alert"><field name="accessWarning"/></p>
                <group attrs="{'invisible': [['displayAccessMode', '=', false]]}">
                    <field class="flex-row" name="accessMode" widget="radio"/>
                </group>
                <group name="shareLink" attrs="{'invisible': [['accessMode', '=', 'edit']]}">
                    <field name="shareLink" widget="CopyClipboardChar" options="{'string': 'Copy Link'}"/>
                </group>
                <group>
                    <div class="o-td-label">
                        <label for="partnerIds" string="Invite People" attrs="{'invisible': [['accessMode', '=', 'read']]}"/>
                        <label for="partnerIds" attrs="{'invisible': [['accessMode', '=', 'edit']]}"/>
                    </div>
                    <field name="partnerIds" widget="many2manyTagsEmail" placeholder="Add contacts to share the project..." nolabel="1"/>
                </group>
                <group>
                    <field name="note" placeholder="Add a note"/>
                </group>
                <footer>
                    <button string="Send" name="actionSendMail" attrs="{'invisible': [['accessWarning', '!=', '']]}" type="object" class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-default" special="cancel" data-hotkey="z" />
                </footer>
            </form>
        </field>
    </record>

    <record id="projectShareWizardAction" model="ir.actions.actwindow">
        <field name="label">Share Project</field>
        <field name="resModel">project.share.wizard</field>
        <field name="bindingModelId" ref="model_projectProject"/>
        <field name="viewMode">form</field>
        <field name="target">new</field>
    </record>

</verp>
