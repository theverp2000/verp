<?xml version="1.0" encoding="utf-8"?>
<verp>
    <record id="ratingRatingViewTreeProject" model="ir.ui.view">
        <field name="label">rating.rating.tree.project</field>
        <field name="model">rating.rating</field>
        <field name="inheritId" ref="rating.ratingRatingViewTree"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <field name="resName" position="attributes">
                <attribute name="string">Task</attribute>
            </field>
            <field name="parentResName" position="attributes">
                <attribute name="string">Project</attribute>
            </field>
            <field name="ratedPartnerId" position="attributes">
                <attribute name="string">Assigned to</attribute>
            </field>
        </field>
    </record>

    <record id="ratingRatingViewFormProject" model="ir.ui.view">
        <field name="label">rating.rating.form.project</field>
        <field name="model">rating.rating</field>
        <field name="inheritId" ref="rating.ratingRatingViewForm"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <field name="resName" position="attributes">
                <attribute name="string">Task</attribute>
            </field>
            <field name="resourceRef" position="attributes">
                <attribute name="string">Task</attribute>
            </field>
            <field name="parentRef" position="attributes">
                <attribute name="string">Project</attribute>
            </field>
            <field name="parentResName" position="attributes">
                <attribute name="string">Project</attribute>
            </field>
            <field name="ratedPartnerId" position="attributes">
                <attribute name="string">Assigned to</attribute>
            </field>
            <div name="ratingImageContainer" position="replace">
                <field name="ratingText" string="Rating"/>
            </div>
            <xpath expr="//field[@name='isInternal']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <record id="ratingRatingViewPivot" model="ir.ui.view">
        <field name="label">rating.rating.view.pivot.project</field>
        <field name="model">rating.rating</field>
        <field name="inheritId" ref="rating.ratingRatingViewPivot"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//pivot" position="attributes">
                <attribute name="jsClass">projectRatingPivot</attribute>
            </xpath>
        </field>
    </record>

    <record id="ratingRatingViewGraph" model="ir.ui.view">
        <field name="label">rating.rating.view.graph.project</field>
        <field name="model">rating.rating</field>
        <field name="inheritId" ref="rating.ratingRatingViewGraph"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//graph" position="attributes">
                <attribute name="jsClass">projectRatingGraph</attribute>
            </xpath>
        </field>
    </record>

    <record id="ratingRatingViewSearchProject" model="ir.ui.view">
        <field name="label">rating.rating.search.project</field>
        <field name="model">rating.rating</field>
        <field name="inheritId" ref="rating.ratingRatingViewSearch"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='resource']" position="after">
                <filter string="Project" name="groupbyProject" context="{'groupby': 'parentResName'}"/>
            </xpath>
            <xpath expr="//filter[@name='responsible']" position="replace"></xpath>
            <xpath expr="/search" position="inside">
                <filter string="Last 30 Days" name="ratingLast30Days" invisible="1" domain="[
                    ['updatedAt', '>=', contextToday().sub({days: 30}).combine('min').toFormat('yyyy-MM-dd HH:mm:ss')]]"
                />
                <separator/>
            </xpath>
        </field>
    </record>

    <record id="ratingRatingActionViewProjectRating" model="ir.actions.actwindow">
        <field name="label">Rating</field>
        <field name="resModel">rating.rating</field>
        <field name="viewMode">kanban,tree,graph,pivot,form</field>
        <field name="domain">[['consumed','=',true], ['parentResModel','=','project.project']]</field>
        <field name="searchViewId" ref="ratingRatingViewSearchProject"/>
        <field name="help" type="html">
            <p class="o-view-nocontent-empty-folder">
                There are no ratings for this project at the moment
            </p>
        </field>
    </record>

    <record id="ratingRatingActionViewProjectRatingTree" model="ir.actions.actwindow.view">
        <field name="sequence" eval="10"/>
        <field name="viewMode">tree</field>
        <field name="actwindowId" ref="ratingRatingActionViewProjectRating"/>
        <field name="viewId" ref="ratingRatingViewTreeProject"/>
    </record>

    <record id="ratingRatingActionViewProjectRatingForm" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">form</field>
        <field name="actwindowId" ref="ratingRatingActionViewProjectRating"/>
        <field name="viewId" ref="ratingRatingViewFormProject"/>
    </record>

    <record id="ratingRatingActionViewProjectRatingPivot" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">pivot</field>
        <field name="actwindowId" ref="ratingRatingActionViewProjectRating"/>
        <field name="viewId" ref="ratingRatingViewPivot"/>
    </record>

    <record id="ratingRatingActionViewProjectRatingGraph" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">graph</field>
        <field name="actwindowId" ref="ratingRatingActionViewProjectRating"/>
        <field name="viewId" ref="ratingRatingViewGraph"/>
    </record>

    <record id="ratingRatingActionTask" model="ir.actions.actwindow">
        <field name="label">Customer Ratings on Task</field>
        <field name="resModel">rating.rating</field>
        <field name="viewMode">kanban,tree,pivot,graph,form</field>
        <field name="domain">[['resModel', '=', 'project.task'], ['resId', '=', activeId], ['consumed', '=', true]]</field>
        <field name="searchViewId" ref="ratingRatingViewSearchProject"/>
        <field name="help" type="html">
            <p class="o-view-nocontent-empty-folder">
                No customer ratings yet
            </p><p>
                Let's wait for your customers to manifest themselves.
            </p>
        </field>
    </record>

    <record id="ratingRatingActionTaskTree" model="ir.actions.actwindow.view">
        <field name="sequence" eval="10"/>
        <field name="viewMode">tree</field>
        <field name="actwindowId" ref="ratingRatingActionTask"/>
        <field name="viewId" ref="ratingRatingViewTreeProject"/>
    </record>

    <record id="ratingRatingActionTaskForm" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">form</field>
        <field name="actwindowId" ref="ratingRatingActionTask"/>
        <field name="viewId" ref="ratingRatingViewFormProject"/>
    </record>

    <record id="ratingRatingActionTaskPivot" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">pivot</field>
        <field name="actwindowId" ref="ratingRatingActionTask"/>
        <field name="viewId" ref="ratingRatingViewPivot"/>
    </record>

    <record id="ratingRatingActionTaskGraph" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">graph</field>
        <field name="actwindowId" ref="ratingRatingActionTask"/>
        <field name="viewId" ref="ratingRatingViewGraph"/>
    </record>

    <record id="ratingRatingActionProjectReport" model="ir.actions.actwindow">
        <field name="label">Customer Ratings</field>
        <field name="resModel">rating.rating</field>
        <field name="viewMode">kanban,tree,pivot,graph,form</field>
        <field name="domain">[['parentResModel','=','project.project'], ['consumed', '=', true]]</field>
        <field name="searchViewId" ref="ratingRatingViewSearchProject"/>
        <field name="help" type="html">
            <p class="o-view-nocontent-empty-folder">
                No customer ratings yet
            </p><p>
                Let's wait for your customers to manifest themselves.
            </p>
        </field>
        <field name="context">{'searchDefault_lastMonth': 1, 'searchDefault_responsible': 1}</field>
    </record>

    <record id="ratingRatingActionProjectReportKanban" model="ir.actions.actwindow.view">
        <field name="sequence" eval="5"/>
        <field name="viewMode">kanban</field>
        <field name="actwindowId" ref="ratingRatingActionProjectReport"/>
        <field name="viewId" ref="rating.ratingRatingViewKanban"/>
    </record>

    <record id="ratingRatingActionProjectReportTree" model="ir.actions.actwindow.view">
        <field name="sequence" eval="10"/>
        <field name="viewMode">tree</field>
        <field name="actwindowId" ref="ratingRatingActionProjectReport"/>
        <field name="viewId" ref="ratingRatingViewTreeProject"/>
    </record>

    <record id="ratingRatingActionProjectReportForm" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">form</field>
        <field name="actwindowId" ref="ratingRatingActionProjectReport"/>
        <field name="viewId" ref="ratingRatingViewFormProject"/>
    </record>

    <record id="ratingRatingActionProjectReportPivot" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">pivot</field>
        <field name="actwindowId" ref="ratingRatingActionProjectReport"/>
        <field name="viewId" ref="ratingRatingViewPivot"/>
    </record>

    <record id="ratingRatingActionProjectReportGraph" model="ir.actions.actwindow.view">
        <field name="sequence" eval="40"/>
        <field name="viewMode">graph</field>
        <field name="actwindowId" ref="ratingRatingActionProjectReport"/>
        <field name="viewId" ref="ratingRatingViewGraph"/>
    </record>

</verp>
