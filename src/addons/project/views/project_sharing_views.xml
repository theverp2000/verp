<?xml version="1.0" encoding="utf-8"?>
<verp>

    <record id="projectSharingQuickCreateTaskForm" model="ir.ui.view">
        <field name="label">project.task.form.quickCreate</field>
        <field name="model">project.task</field>
        <field name="priority">999</field>
        <field name="groupsId" eval="[[4, refId('base.groupPortal')], [4, refId('base.groupUser')]]"/>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="label" string="Task Title" placeholder="e.g. New Design"/>
                </group>
            </form>
        </field>
    </record>

    <record id="projectSharingProjectTaskViewKanban" model="ir.ui.view">
        <field name="label">project.sharing.project.task.view.kanban</field>
        <field name="model">project.task</field>
        <field name="priority">999</field>
        <field name="groupsId" eval="[[4, refId('base.groupPortal')], [4, refId('base.groupUser')]]"/>
        <field name="arch" type="xml">
            <kanban
                class="o-kanban-small-column o-kanban-project-tasks"
                defaultGroupby="stageId"
                onCreate="quickCreate"
                quickCreateView="project.projectSharingQuickCreateTaskForm"
                archivable="0"
                import="0"
            >
                <field name="color"/>
                <field name="priority"/>
                <field name="stageId" options='{"groupby_tooltip": {"description": "Description"}}'/>
                <!-- TODO: [XBO] remove me in master -->
                <field name="userIds" invisible="1"/>
                <field name="portalUserNames"/>
                <field name="partnerId"/>
                <field name="sequence"/>
                <field name="isClosed"/>
                <field name="partnerIsCompany"/>
                <field name="displayedImageId"/>
                <field name="active"/>
                <field name="allowSubtasks"/>
                <field name="childText"/>
                <field name="legendBlocked" invisible="1"/>
                <field name="legendNormal" invisible="1"/>
                <field name="legendDone" invisible="1"/>
                <progressbar field="kanbanState" colors='{"done": "success", "blocked": "danger", "normal": "muted"}'/>
                <templates>
                <t t-name="kanban-box">
                    <div t-attf-class="{{!selectionMode ? 'oe-kanban-color-' + kanbanGetcolor(record.color.rawValue) : ''}} oe-kanban-card oe-kanban-global-click">
                        <div class="oe-kanban-content">
                            <div class="o-kanban-record-top">
                                <div class="o-kanban-record-headings">
                                    <strong class="o-kanban-record-title">
                                        <s t-if="!record.active.rawValue"><field name="label" widget="nameWithSubtaskCount"/></s>
                                        <t t-else=""><field name="label" widget="nameWithSubtaskCount"/></t>
                                    </strong>
                                    <span invisible="(context['default_projectId'] ?? false)"><br/><field name="projectId" required="1"/></span>
                                    <br />
                                    <t t-if="record.partnerId.value">
                                        <span t-if="!record.partnerIsCompany.rawValue">
                                            <field name="commercialPartnerId"/>
                                        </span>
                                        <span t-else="">
                                            <field name="partnerId"/>
                                        </span>
                                    </t>
                                    <t t-else="record.emailFrom.rawValue"><span><field name="emailFrom"/></span></t>
                                </div>
                                <div class="o-dropdown-kanban dropdown" t-if="!selectionMode">
                                    <a role="button" class="dropdown-toggle o-no-caret btn" data-toggle="dropdown" data-display="static" href="#" aria-label="Dropdown menu" title="Dropdown menu">
                                        <span class="fa fa-ellipsis-v"/>
                                    </a>
                                    <div class="dropdown-menu" role="menu">
                                        <!-- [XBO] TODO: remove me in master -->
                                        <a t-if="widget.editable" role="menuitem" type="setCover" class="dropdown-item d-none" data-field="displayedImageId">Set Cover Image</a>
                                        <a t-if="widget.editable" role="menuitem" type="edit" class="dropdown-item">Edit</a>
                                        <div role="separator" class="dropdown-divider"></div>
                                        <ul class="oe-kanban-colorpicker" data-field="color"/>
                                    </div>
                                </div>
                            </div>
                            <div class="o-kanban-record-body">
                                <field name="tagIds" widget="many2manyTags" options="{'colorField': 'color'}"/>
                                <div t-if="record.dateDeadline.rawValue" name="dateDeadline" attrs="{'invisible': [['isClosed', '=', true]]}">
                                    <field name="dateDeadline" widget="remainingDays"/>
                                </div>
                                <div t-if="record.displayedImageId.value">
                                    <field name="displayedImageId" widget="attachmentImage"/>
                                </div>
                            </div>
                            <div class="o-kanban-record-bottom" t-if="!selectionMode">
                                <div class="oe-kanban-bottom-left">
                                    <field name="priority" widget="priority"/>
                                </div>
                                <div class="oe-kanban-bottom-right" t-if="!selectionMode">
                                    <span t-if="record.portalUserNames.rawValue.length > 0" class="pr-2" t-att-title="record.portalUserNames.rawValue">
                                        <t t-set="userCount" t-value="record.portalUserNames.rawValue.split(',').length"/>
                                        <t t-out="userCount"/>
                                        <t t-if="userCount > 1"> assignees</t>
                                        <t t-else=""> assignee</t>
                                        <t t-if="1 == 0" t-set="displayNbAssignees" t-value="userCount + ' assignee' + (userCount > 1 ? 's' : '')"/>
                                        <t t-if="1 == 0" t-out="displayNbAssignees"/>
                                    </span>
                                    <field name="kanbanState" widget="stateSelection"/>
                                    <!-- TODO: [XBO] remove me in master -->
                                    <field name="userIds" string="Assignees" widget="many2manyAvatarUser" options="{'noOpenChat': true}" invisible="1"/>
                                </div>
                            </div>
                        </div>
                        <div class="oe-clear"></div>
                    </div>
                </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="projectSharingProjectTaskViewTree" model="ir.ui.view">
        <field name="label">project.sharing.project.task.tree</field>
        <field name="model">project.task</field>
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <tree string="Tasks" sample="1" delete="0" import="0">
                <field name="isClosed" invisible="1" />
                <field name="allowSubtasks" invisible="1" />
                <field name="sequence" invisible="1" readonly="1"/>
                <field name="priority" widget="priority" optional="show" nolabel="1"/>
                <field name="label" widget="nameWithSubtaskCount"/>
                <field name="childText" invisible="1"/>
                <field name="companyId" invisible="1"/>
                <field name="partnerId" optional="hide"/>
                <field name="portalUserNames" string="Assignees" optional="show"/>
                <!-- TODO: [XBO] remove me in master -->
                <field name="userIds" invisible="1" />
                <field name="dateDeadline" optional="hide" widget="remainingDays" attrs="{'invisible': [['isClosed', '=', true]]}"/>
                <field name="tagIds" widget="many2manyTags" options="{'colorField': 'color'}" optional="show"/>
                <field name="kanbanState" widget="stateSelection" optional="hide"/>
                <field name="legendBlocked" invisible="1"/>
                <field name="legendNormal" invisible="1"/>
                <field name="legendDone" invisible="1"/>
                <field name="stageId" invisible="(context['setVisible'] ?? false)" optional="show"/>
            </tree>
        </field>
    </record>

    <record id="projectSharingProjectTaskViewForm" model="ir.ui.view">
        <field name="label">project.sharing.project.task.view.form</field>
        <field name="model">project.task</field>
        <field name="priority">999</field>
        <field name="groupsId" eval="[[4, refId('base.groupPortal')], [4, refId('base.groupUser')]]"/>
        <field name="arch" type="xml">
            <form string="Project Sharing: Task" class="o-form-project-tasks">
                <header>
                    <button name="actionAssignToMe" string="Assign to Me" type="object" class="oe-highlight"
                            attrs="{'invisible' : &quot;[['userIds', 'in', [uid]]]&quot;}" data-hotkey="q" groups="base.groupUser"/>
                    <button name="actionUnassignMe" string="Unassign Me" type="object" class="oe-highlight"
                            attrs="{'invisible' : &quot;[['userIds', 'not in', [uid]]]&quot;}" data-hotkey="q"/>
                    <field name="stageId" widget="statusbar" options="{'clickable': '1', 'foldField': 'fold'}" attrs="{'invisible': [['projectId', '=', false], ['stageId', '=', false]]}" />
                </header>
                <sheet string="Task">
                    <div class="oe-button-box" name="buttonBox">
                        <button name="actionOpenParentTask" type="object" special="cancel" class="oe-stat-button o-debounce-disabled" icon="fa-tasks" string="Parent Task" attrs="{'invisible': [['parentId', '=', false]]}"/>
                    </div>
                    <widget name="webRibbon" title="Archived" bgcolor="bg-danger" attrs="{'invisible': [['active', '=', true]]}"/>
                    <div class="oe-title pr-0">
                        <h1 class="d-flex flex-row justify-content-between">
                            <field name="priority" widget="priority" class="mr-3"/>
                            <field name="label" class="o-task-name text-truncate" placeholder="Task Title..."/>
                            <field name="kanbanState" widget="stateSelection" class="ml-auto"/>
                            <field name="legendBlocked" invisible="1"/>
                            <field name="legendNormal" invisible="1"/>
                            <field name="legendDone" invisible="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="projectId" invisible="1"/>
                            <field name="displayProjectId" string="Project" invisible="1"/>
                            <field name="userIds" invisible="1" />
                            <field name="portalUserNames"
                                string="Assignees"
                                class="o-task-user-field"/>
                        </group>
                        <group>
                            <field name="active" invisible="1"/>
                            <field name="parentId" invisible="1" />
                            <field name="companyId" invisible="1" />
                            <field name="isClosed" invisible="1" />
                            <field name="allowSubtasks" invisible="1" />
                            <field name="partnerId" options="{'noOpen': true, 'noCreate': true, 'noEdit': true}"/>
                            <field name="dateDeadline" attrs="{'invisible': [['isClosed', '=', true]]}"/>
                            <field name="tagIds" widget="many2manyTags" options="{'colorField': 'color', 'noCreateEdit': true, 'noEditColor': true}"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="descriptionPage" string="Description">
                            <field name="description" type="html" options="{'collaborative': true}"/>
                        </page>
                        <page name="subTasksPage" string="Sub-tasks" attrs="{'invisible': [['allowSubtasks', '=', false]]}">
                            <field name="childIds" context="{'default_projectId': !bool(parentId) || !displayProjectId ? projectId : displayProjectId, 'default_parentId': id, 'default_partnerId': partnerId, 'formViewRef' : 'project.projectSharingProjectTaskViewForm'}">
                                <tree editable="bottom">
                                    <field name="projectId" invisible="1"/>
                                    <field name="isClosed" invisible="1"/>
                                    <field name="sequence" widget="handle"/>
                                    <field name="label"/>
                                    <field name="displayProjectId" string="Project" optional="hide" invisible="1"/>
                                    <field name="companyId" invisible="1"/>
                                    <field name="partnerId" options="{'noOpen': true, 'noCreate': true, 'noEdit': true}" optional="hide"/>
                                    <!-- TODO: [XBO] remove me in master -->
                                    <field name="userIds" invisible="1"/>
                                    <field name="portalUserNames" string="Assignees"/>
                                    <field name="dateDeadline" attrs="{'invisible': [['isClosed', '=', true]]}" optional="show"/>
                                    <field name="tagIds" widget="many2manyTags" options="{'colorField': 'color'}" optional="hide"/>
                                    <field name="kanbanState" widget="stateSelection" optional="hide"/>
                                    <field name="stageId" optional="show"/>
                                    <button name="actionOpenTask" type="object" title="View Task" string="View Task" class="btn btn-link pull-right"
                                            context="{'formViewRef': 'project.projectSharingProjectTaskViewForm'}"
                                            attrs="{'invisible': &quot;[['displayProjectId', '!=', false], ['displayProjectId', '!=', activeId]]&quot;}"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe-chatter"/>
            </form>
        </field>
    </record>

    <record id="projectSharingProjectTaskViewSearch" model="ir.ui.view">
        <field name="label">project.task.search.form</field>
        <field name="model">project.task</field>
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <search string="Tasks">
                <field name="label" string="Task"/>
                <field name="tagIds"/>
                <!-- TODO: [XBO] remove me in master -->
                <field name="userIds" invisible="1"/>
                <field name="portalUserNames" string="Assignees"/>
                <field name="partnerId" operator="childOf"/>
                <field name="stageId"/>
                <field string="Project" name="displayProjectId"/>
                <filter string="Unassigned" name="unassigned" domain="[['userIds', '=', false]]"/>
                <separator/>
                <filter string="Starred" name="starred" domain="[['priority', 'in', [1, 2]]]"/>
                <separator/>
                <filter string="Late Tasks" name="late" domain="[['dateDeadline', '&lt;', toFormat(contextToday(),'yyyy-MM-dd')], ['isClosed', '=', false]]"/>
                <separator/>
                <filter invisible="1" string="Today Activities" name="activitiesToday"
                    domain="[['activityIds.dateDeadline', '=', toFormat(contextToday(), 'yyyy-MM-dd')]]"/>
                <filter invisible="1" string="Future Activities" name="activitiesUpcomingAll"
                    domain="[['activityIds.dateDeadline', '&gt;', toFormat(contextToday(), 'yyyy-MM-dd')]]"/>
                <group expand="0" string="Group By">
                    <filter string="Stage" name="stage" context="{'groupby': 'stageId'}"/>
                    <!-- TODO: [XBO] remove me in master -->
                    <filter string="Assignees" name="user" context="{'groupby': 'userIds'}" invisible="1"/>
                    <!-- [XBO] TODO: remove me in master -->
                    <filter string="Project" name="project" context="{'groupby': 'projectId'}" invisible="1"/>
                    <filter string="Customer" name="customer" context="{'groupby': 'partnerId'}"/>
                    <filter string="Kanban State" name="kanbanState" context="{'groupby': 'kanbanState'}"/>
                    <filter string="Deadline" name="dateDeadline" context="{'groupby': 'dateDeadline'}"/>
                    <!-- [XBO] TODO: remove me in master -->
                    <filter string="Creation Date" name="groupCreateDate" context="{'groupby': 'createdAt'}" invisible="1"/>
                </group>
            </search>
        </field>
    </record>

    <record id="projectSharingProjectTaskAction" model="ir.actions.actwindow">
        <field name="label">Project Sharing</field>
        <field name="resModel">project.task</field>
        <field name="viewMode">kanban,tree,form</field>
        <field name="searchViewId" ref="project.projectSharingProjectTaskViewSearch"/>
        <field name="domain">[['displayProjectId', '=', activeId]]</field>
        <field name="context">{
            'default_projectId': activeId,
            'delete': 0,
        }</field>
        <field name="help" type="html">
            <p class="o-view-nocontent-smiling-face">
                No tasks found. Let's create one!
            </p><p>
                To get things done, use activities and status on tasks.<br/>
                Chat in real time or by email to collaborate efficiently.
            </p>
        </field>
    </record>

    <record id="projectSharingKanbanActionView" model="ir.actions.actwindow.view">
        <field name="viewMode">kanban</field>
        <field name="actwindowId" ref="project.projectSharingProjectTaskAction"/>
        <field name="viewId" ref="project.projectSharingProjectTaskViewKanban"/>
    </record>

    <record id="projectSharingTreeActionView" model="ir.actions.actwindow.view">
        <field name="viewMode">tree</field>
        <field name="actwindowId" ref="project.projectSharingProjectTaskAction"/>
        <field name="viewId" ref="project.projectSharingProjectTaskViewTree"/>
    </record>

    <record id="projectSharingFormActionView" model="ir.actions.actwindow.view">
        <field name="viewMode">form</field>
        <field name="actwindowId" ref="project.projectSharingProjectTaskAction"/>
        <field name="viewId" ref="project.projectSharingProjectTaskViewForm"/>
    </record>

</verp>
