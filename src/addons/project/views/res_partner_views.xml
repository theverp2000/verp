<?xml version="1.0" encoding="utf-8"?>
<verp>

         <!--  Partners inherited form -->
        <record id="viewTaskPartnerInfoForm" model="ir.ui.view">
            <field name="label">res.partner.task.buttons</field>
            <field name="model">res.partner</field>
            <field name="inheritId" ref="base.viewPartnerForm"/>
            <field name="priority" eval="7"/>
            <field name="groupsId" eval="[[4, refId('project.groupProjectUser')]]"/>
            <field name="arch" type="xml">
                <div name="buttonBox" position="inside">
                    <button class="oe-stat-button" type="action" name="%(projectTaskActionFromPartner)d"
                        context="{'searchDefault_partnerId': activeId, 'default_partnerId': activeId}" attrs="{'invisible': [['taskCount', '=', 0]]}"
                        icon="fa-tasks">
                        <field  string="Tasks" name="taskCount" widget="statinfo"/>
                    </button>
                </div>
            </field>
       </record>

</verp>
