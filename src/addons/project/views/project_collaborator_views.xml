<?xml version="1.0" encoding="utf-8"?>
<verp>

    <record id="projectCollaboratorViewForm" model="ir.ui.view">
        <field name="label">project.collaborator.view.form</field>
        <field name="model">project.collaborator</field>
        <field name="arch" type="xml">
            <form string="Project Collaborator" create="0" edit="0">
                <sheet>
                    <group>
                        <field name="projectId" options="{'noCreate': true}" />
                        <field name="partnerId" options="{'noCreate': true}" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="projectSharingAccessViewTree" model="ir.ui.view">
        <field name="label">project.collaborator.view.tree</field>
        <field name="model">project.collaborator</field>
        <field name="arch" type="xml">
            <tree string="Project Collaborators" create="0">
                <field name="projectId" options="{'noCreate': true}"/>
                <field name="partnerId" options="{'noCreate': true}"/>
            </tree>
        </field>
    </record>

    <record id="projectCollaboratorViewSearch" model="ir.ui.view">
        <field name="label">project.collaborator.view.search</field>
        <field name="model">project.collaborator</field>
        <field name="arch" type="xml">
            <search>
                <field name="partnerId" />
                <field name="projectId" />
                <group expand="0" string="Group By">
                    <filter name="project" string="Project" context="{'groupby': 'projectId'}" />
                    <filter name="collaborator" string="Collaborator" context="{'groupby': 'partnerId'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="projectCollaboratorAction" model="ir.actions.actwindow">
        <field name="label">Project Collaborators</field>
        <field name="resModel">project.collaborator</field>
        <field name="viewMode">tree,form</field>
        <field name="domain">[['projectId', '=', activeId]]</field>
        <field name="searchViewId" ref="projectCollaboratorViewSearch"/>
    </record>

</verp>
